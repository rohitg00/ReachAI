services:
  reachai:
    build:
      context: .
    ports:
      - "3111:3111"
    volumes:
      - ./.env:/app/reachai-backend/.env:ro
      - reachai_data:/app/reachai-backend/data
      - iii_home:/home/node/.iii
    environment:
      - RUST_LOG=info
    stop_signal: SIGINT
    stop_grace_period: 30s
    restart: unless-stopped

volumes:
  reachai_data:
  iii_home:
